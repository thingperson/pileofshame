# Decisions Audit — 2026-06-22

**Mode:** `regress-watch` decisions-audit (Agent B, autonomous weekly run)
**Scope:** 6 `decision-*` assertions checked against working-tree code. DECISIONS.md carries 80+ dated decision entries (newest 2026-06-17 — light-theme accent vars + iOS Steam OpenID realm); the 6 assertions encode the LOCKED entries deemed code-auditable. Locked-but-unasserted entries listed at the bottom. No new DECISIONS entries since the 2026-06-15 audit, so the assertion set is unchanged.

**Headline:** 5 holds, 1 drift (unauthorized), 0 can't-tells. **Last week's drift carries over:** the retired status label "Play Next" in `JustFiveMinutes.tsx:262` is still present and unfixed (second consecutive flag). Everything else holds — last week's "What Should I Play?" → "Pick My Game" copy sweep is now **committed** (repo is clean; working-tree edits from the 2026-06-15 run landed in HEAD `cccb71f`).

> **Note:** repo had only **untracked** files at audit time (`docs/audits/` prior reports, `.playwright-mcp/`, `public/if-logos/inventory-full-logo-1024-square.png`). No modified tracked files — the in-progress edits flagged in the 2026-06-15 audit (HelpModal, PostImportSummary, page.tsx, privacy, the LandingPageClassic archival rename) are now committed. Audit ran against working-tree state. No code modified; no push.

---

## Per-assertion verdict

### ❌ `decision-status-cycle-locked` — Drift (unauthorized · repeat)
Cites DECISIONS 2026-04-09 (status cycle locked: Backlog → Up Next → Playing Now → Completed, Moved On sibling).
- **Internal keys intact:** `lib/types.ts:3` → `'buried' | 'on-deck' | 'playing' | 'played' | 'bailed'`. Unchanged.
- **Canonical labels intact in the source of truth:** `lib/constants.ts:45–49` maps `buried→Backlog`, `on-deck→Up Next`, `playing→Playing Now`, `played→Completed`, `bailed→Moved On`. GameCard/GridCard pills consume these. ✅
- **Drift (carried over from 2026-06-15, still unfixed):** `components/JustFiveMinutes.tsx:262` renders a user-facing triage button reading **"📋 Play Next"** wired to `on-deck`. "Play Next" is an **explicitly retired label** (voice-charter.md terminology table + this assertion's retired list). The `on-deck` status must read **"Up Next"** (per `constants.ts:46`). Unauthorized — no DECISIONS entry preserves "Play Next." This was flagged last week with a one-line fix recommendation; it has not been actioned.
  - **Recommend:** `JustFiveMinutes.tsx:262` "📋 Play Next" → "📋 Up Next". One-line fix.
- **Soft watch (carryover):** `components/StatsPanel.tsx:458` renders `<StatCard label="Cleared" …>`. "Cleared" is the retired label for *Completed*, used here as a metric name, not a status pill. Borderline off-terminology; consider "Completed." (Internal identifiers `gamesCleared` at `:153,175,370,413` are fine — variable names, not user-facing.)

### ✅ `decision-picker-cta-pick-my-game` — Holds (last week's drift now committed)
Cites DECISIONS 2026-05-05 (CTA renamed "What Should I Play?" → "Pick My Game").
- **Primary CTA holds:** "Pick My Game" appears **14×** across `app/` + `components/` (hero, post-import, Reroll header + `aria-label`, JSON-LD).
- **Second clause passes:** `What Should I Play?` returns **0 user-facing occurrences** (grep across `app/`, `components/`). The 2026-06-15 working-tree fix (HelpModal + PostImportSummary sweep) is now committed to HEAD. Button and surrounding copy agree.

### ✅ `decision-moving-on-canon` — Holds
Cites voice-charter.md ("Moving on is deciding too"; sentiment locked, variations OK).
- Anchor present: `LandingPageV2.tsx:466` — "Moving on is deciding too."
- In-app affirmation pool intact: `GameCard.tsx:532–533` ("You drew a line. That takes guts." / "Knowing when to walk away is a superpower."). Button "I'm moving on" (`:1289`). All sentiment-positive.
- No retired user-facing vocab (Bailed/Dropped/Abandoned/Quit on it) in `components/` or `app/`; `bailed`/`bailedCount` are internal identifiers only.

### ✅ `decision-pick-flow-two-inputs` — Holds
Cites DECISIONS 2026-04-28 + user-psychology.md (2-input ceiling: mood + session length).
- `components/Reroll.tsx`: two axes present — mood (`toggleMood`, `:100`) and session length (`SessionLength`/`sessionLength`/`showSessionLengthDrawer`, `:12,62,77`). Telemetry carries `session_length` (`:178`).
- **No prohibited third axis at pick-time.** `platformPreference` (`:88`) is a persisted *setting* read from the store and passed into `getEligibleGames` as a background filter (`:106`) — not a pick-flow input group surfaced in the pre-roll modal. No genre/length filter controls. Cap holds.
- Cosmetic carryover: comment at `Reroll.tsx:57` still says "energy pills" — stale pre-pivot wording (DECISIONS 2026-04-27 PM). Harmless.

### ✅ `decision-playing-now-cap-3` — Holds (strong)
Cites DECISIONS 2026-04-06 (Playing Now cap = 3, every entry point).
- `lib/constants.ts:59` → `export const MAX_PLAYING_NOW = 3;`
- Enforced at every promotion entry point: `components/Reroll.tsx:220–221`, `components/JustFiveMinutes.tsx:118–119`, `components/GameCard.tsx:424–425`. Each blocks the 4th with the toast "Playing Now is capped at 3. Finish or shelve something first."

### ✅ `decision-tagline-canon` — Holds (one soft watch · carryover)
Cites voice-charter.md (primary `get playing.`; landing H1 "Your backlog isn't the problem. Deciding is."; "Stop stalling" + "Your pile's not gonna play itself." retired).
- **Primary tagline holds:** `get playing.` (lowercase, period) present **20×** across titles/OG/footers.
- **Active landing H1 holds:** `app/page.tsx` renders `LandingPageV2`; hero matches voice-charter 2026-05-11.
- **Retired literals absent:** "Stop stalling" — 0 occurrences.
- **Soft watch (carryover from 2026-06-15):** `app/pile/[id]/page.tsx:255` CTA reads **"Your backlog's not gonna play itself."** — a reworded (pile→backlog) version of the retired "Your pile's not gonna play itself." sentiment. Not a literal match to the banned string, and it lives on a secondary share-card landing surface, so the assertion as written holds. The assertion's surface list does include "share cards" and voice-charter put this sentiment in the retired column. Unchanged from last week — still awaiting Brady's call: bless the reworded variant or swap to canonical framing.

---

## Suggested follow-ups

**Code to sweep (the one real drift — now a repeat):**
1. `JustFiveMinutes.tsx:262` — change "📋 Play Next" → "📋 Up Next". Retired status label as a live triage button; canonical `on-deck` label is "Up Next" (`constants.ts:46`). **Flagged 2026-06-15, still unfixed — second consecutive audit.** One-line fix.

**Soft / Brady's call (carryovers):**
2. `app/pile/[id]/page.tsx:255` — "Your backlog's not gonna play itself." is a reworded retired tagline on the pile share-card CTA. Bless it or swap to canonical framing.
3. `StatsPanel.tsx:458` — "Cleared" stat label → "Completed" for status-cycle terminology consistency.
4. `Reroll.tsx:57` — comment "energy pills" is stale pre-pivot wording; tidy when next in the file.

**Assertions to refine:**
5. `decision-status-cycle-locked` — add `components/JustFiveMinutes.tsx` to the assertion's named surface list. The 2026-06-15 audit recommended this; not yet reflected in `assertions.md`. (The current grep widened to all of `components/` and caught it both runs, so it's a documentation tidy, not a coverage gap.)

**No assertion yet — add one if this should be audited** *(unchanged from 2026-06-15; all grep-auditable except where noted):*
- 2026-05-05 — "From your shelf" similar games fires only on Completed modal (`GameCard.tsx`, gated `status === 'played' && forceExpanded`).
- 2026-05-05 — Modal destructive-action disclosure (Delete/Don't Suggest behind "More actions"; Bail always visible).
- 2026-05-13 — `DONE_FLAVORS` separate share-copy pool for `isNonFinishable` games (`CompletionCelebration.tsx`).
- 2026-05-05 — `navigator.share` archetype payload `{title, url}` only, no `text` (`ArchetypeCard.tsx`).
- 2026-05-15 — Sort Option B killed (no user progress self-report field on Playing Now cards; absence check).
- 2026-06-17 — Steam "Sign in through Steam" (OpenID 2.0) as primary import flow + `/steam-return` realm passthrough. Auditable (route existence + `check_authentication` server-side), but spans API routes; candidate if Brady wants Steam-auth covered.
- 2026-04-26 — North Star + AGENTS.md #7 (never auto-assign status from data signals). Too abstract/cross-surface for a `decision-*` — belongs in `psychology-redteam` / `feature-creep-audit`. Listed for completeness, not recommended.
- 2026-05-20 — iOS app decisions (separate repo `inventoryfull-ios`). Out of scope for this repo's code audit.

---

*Audit run: `regress-watch` decisions-audit mode, autonomous weekly cadence. Working tree at commit `cccb71f` (repo had only untracked files at audit time — see note above; HEAD advanced from `1ae1678` at the 2026-06-15 run, which committed last week's CTA-sweep working-tree edits). Cheapest-primitive-first (grep > read); no preview server started. No code modified; no push. Brady decides what to act on.*
