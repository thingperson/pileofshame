# Decisions Audit — 2026-07-06

**Mode:** `regress-watch` decisions-audit (Agent B, weekly cadence)
**Scope:** 6 of 6 `decision-*` assertions audited. All correspond to LOCKED / committed-direction entries in `docs/DECISIONS.md` (+ `voice-charter.md`). Verification by `Bash` grep / code-read primitives only — no live preview (autonomous run; SKILL.md says don't start a dev server unprompted).

**Headline:** 6 holds, 0 hard drifts, 0 can't-tells. Two persistent soft flags carried over from the 2026-06-29 audit (a retired "Play Next" button label; a retired tagline variant on the pile share page) — both unchanged, both still Brady's call. No new drift introduced since last audit.

**Since last audit (cccb71f → 53b6745):** 17 commits, all docs / server-side / data-layer (Supabase enrichment-load fix, GA4 tag fix, status-event log, dead-recommender deletion). **None touch the six audited user-facing surfaces.** The audited state is byte-identical to 2026-06-29.

---

## Per-assertion verdict

| # | Assertion | Verdict | One-line |
|---|---|---|---|
| 1 | `decision-status-cycle-locked` | ✅ Holds | Internal keys + canonical labels intact; carry-over soft flag on the "Play Next" button. |
| 2 | `decision-picker-cta-pick-my-game` | ✅ Holds | "Pick My Game" everywhere, "What Should I Play?" gone from UI, `R` shortcut wired. |
| 3 | `decision-moving-on-canon` | ✅ Holds | Anchor line live; agency-positive affirmation pool; no shame vocab. |
| 4 | `decision-pick-flow-two-inputs` | ✅ Holds | Picker surfaces exactly mood + session length; no genre/platform/length filter. |
| 5 | `decision-playing-now-cap-3` | ✅ Holds | Cap = 3, enforced at every promotion site (assertion method still mis-targeted — see follow-ups). |
| 6 | `decision-tagline-canon` | ✅ Holds (named surfaces) | H1/primary/celebration correct; ⚠️ retired "not gonna play itself" variant still live on pile share page (off-surface). |

---

### 1. `decision-status-cycle-locked` — ✅ Holds
**Cites:** DECISIONS 2026-04-09 (status cycle lock).
- Internal keys unchanged: `'buried' | 'on-deck' | 'playing' | 'played' | 'bailed'` — `lib/types.ts:3`.
- User-facing labels canonical in `components/TabNav.tsx:22–25`: Backlog / Up Next / Playing Now / Completed. Moved On lives as the sibling exit on the Completed tab (`statuses: ['played', 'bailed']`).
- **Soft flag (review, carried over):** `components/JustFiveMinutes.tsx:262` still renders a button labeled **"📋 Play Next"** — `Play Next` is a retired label (superseded by "Up Next"). The handler correctly triages to `'on-deck'`, so it's a label-only slip, not a state-model drift. One-word fix to "Up Next". Same finding as 2026-06-29; not yet actioned.
- **Non-issue (noted for the record):** "buried" / "ON DECK" / "BURIED" appearing in `lib/sampleLibrary.ts` are internal status-key values and code comments, not user-facing labels. "Cleared" in celebration copy is an authorized `CLEAR_FLAVORS` verb (DECISIONS 2026-05-13), not a status label. No drift.

### 2. `decision-picker-cta-pick-my-game` — ✅ Holds
**Cites:** DECISIONS 2026-05-05 (CTA rename).
- `Pick My Game` present: `Reroll.tsx:450` (aria-label) + `Reroll.tsx:470` (button), `PostImportSummary.tsx:107,142`, `HelpModal.tsx:47,65,69,116`, `app/layout.tsx:180` (JSON-LD feature list).
- `What Should I Play` — **zero user-facing hits.** The one match, `app/cant-decide-what-to-play/page.tsx:69`, is an FAQ *question* string ("What should I play when I have too many games?"), not the picker CTA. Not a violation.
- `R` keyboard shortcut wired: `hooks/useKeyboardShortcuts.ts:44` (`e.key === 'r' || 'R'` → `onRoll()`).

### 3. `decision-moving-on-canon` — ✅ Holds
**Cites:** `voice-charter.md` (Moved On canon).
- Anchor "Moving on is deciding too." live on landing (`components/LandingPageV2.tsx:466`).
- Moved-On toast draws from `bailAffirmations` (`GameCard.tsx:532–534`): "You drew a line. That takes guts." / "Knowing when to walk away is a superpower." — agency-positive.
- Modal copy: "I'm moving on" / "Moving on from this game for good?" (`GameCard.tsx:1357,1364`).
- No shame vocab (`Abandoned`, `Quit on it`, `Dropped`, "gave up") in the moved-on surfaces.

### 4. `decision-pick-flow-two-inputs` — ✅ Holds
**Cites:** DECISIONS / `user-psychology.md` (2-input ceiling, shipped 2026-04-28).
- `components/Reroll.tsx` pre-roll has exactly two user-facing inputs: mood (`moodFilters`, line 53) and session length (`sessionLength`, line 78).
- `platformPreference` (line 89) is read from persistent Settings (`useStore((s) => s.settings.platformPreference)`) — not a pick-time input group, doesn't count against the ceiling.
- No genre filter, no platform filter, no separate length filter rendered in the picker.
- *Confidence note:* verified by control inventory in source; a live `eval` counting rendered input groups in the open modal would be the belt-and-suspenders confirmation.

### 5. `decision-playing-now-cap-3` — ✅ Holds (assertion method needs refinement)
**Cites:** DECISIONS 2026-04-06 (Playing Now cap of 3).
- `MAX_PLAYING_NOW = 3` — `lib/constants.ts:59`.
- Enforced before every promotion to `playing`: `GameCard.tsx:425` and `:566`, `Reroll.tsx:221`, `JustFiveMinutes.tsx:118` all guard `nowPlayingCount >= MAX_PLAYING_NOW` and block + toast.
- **The cap holds**, but the assertion's stated method ("grep `lib/store.ts` for cap logic; if none, hard fail") is **mis-targeted** — enforcement lives at the component call sites, not the store. A literal reading would produce a false hard-fail. Same note as last week; see follow-ups.

### 6. `decision-tagline-canon` — ✅ Holds on named surfaces; ⚠️ off-surface soft drift
**Cites:** `voice-charter.md` (taglines, landing H1 locked 2026-05-11).
- Primary `get playing.` present across landing (`LandingPageV2.tsx:123–124`, `NinetiesMode.tsx:445`), OG card (`app/opengraph-image.tsx:118`), layout title (`app/layout.tsx:42`).
- Landing H1 = "Your backlog isn't the problem." / "Deciding is." — `LandingPageV2.tsx:238,241`. Matches canon exactly.
- Celebration "Less shame. More game." present on share pages (`pile/[id]/page.tsx:77`, `clear/[id]/page.tsx:56,176`).
- Retired "Stop stalling" — **absent.**
- **⚠️ Soft flag (review, carried over):** retired tagline variant **"Your backlog's not gonna play itself."** is still live on the pile share page — `app/pile/[id]/page.tsx:255`. Voice-charter retired "Your pile's not gonna play itself."; this is the same retired line with pile→backlog. It's *outside* the assertion's scoped surface ("active landing surface" = `LandingPageV2.tsx`), so the assertion technically passes — but a retired tagline is shipped on a recipient-facing share card. Unchanged since 2026-06-29. Brady's call whether to swap it. *(Note: `GameCard.tsx:454`'s "isn't going to play itself" is a per-game "Let's go" launch toast, a different line — not the retired tagline.)*

---

## No assertion yet — add one if this should be audited

Committed-direction DECISIONS entries that are code-auditable but have no `decision-*` assertion. **Two new candidates this week** (both verified holding in passing):

- **2026-06-30 — Deleted the completion-recommender.** `lib/recommendations.ts` is gone; zero hits for `getCompletionRecommendations` / `getWishlistRecommendations` across `lib/` + `components/`. **Confirmed holding.** This decision protects two *other* locked rules (one-pick/no-shortlist psychology + no-sell legal), so a regression here would be a double violation. Strong assertion candidate: grep for absence of both function names.
- **2026-07-02 — Enrichment converges on `enrichedAt`; field-based filters retired.** All three enrichment sites gate on `!g.enrichedAt` alone (`lib/enrichGame.ts:231`, `hooks/useAutoEnrich.ts:37`, `components/SettingsMenu.tsx:56`); no `!description` / `!moodTags` / `!hltbMain` filters reintroduced. **Confirmed holding.** The DECISIONS entry names this as an explicit drift risk (re-adding those filters was the free-tier-saturation bug). Strong assertion candidate: grep the three files for the forbidden field-based filters.

Carried-over candidates from prior audits (still un-asserted):

- **2026-05-15 — Sort Option B dropped** (no "How far along are you?" progress field on Playing Now cards).
- **2026-05-05 — Similar games strip only on Completed modal** (gate `status === 'played' && forceExpanded` in `GameCard.tsx`).
- **2026-05-05 — Modal destructive-action disclosure** (Delete + Don't Suggest behind "··· More actions"; Bail always visible).
- **2026-05-13 — Light theme as default** (`lib/store.ts` initial state = `light`).

iOS-app decisions (2026-05-20, 2026-06-17 OpenID/steam-return) live in the separate `inventoryfull-ios` repo and per the web-repo scope boundary are intentionally **not** audited here.

---

## Suggested follow-ups (assertion library)

1. **Refine `decision-playing-now-cap-3` method.** Change verification from "grep `lib/store.ts`" to "grep `lib/constants.ts` for `MAX_PLAYING_NOW = 3` **and** confirm a `>= MAX_PLAYING_NOW` guard at each promotion call site (`GameCard`, `Reroll`, `JustFiveMinutes`)." Current method would false-hard-fail because enforcement isn't centralized in the store. **Flagged 2 weeks running — worth the 2-minute edit.**
2. **Scope decision on `decision-tagline-canon`.** Decide whether the pile share page (`app/pile/[id]/page.tsx`) counts as an audited tagline surface. If yes, the retired "...not gonna play itself." line is a fail and should be swapped. If no, add an explicit exclusion note so future runs stop re-flagging it. **Flagged 2 weeks running.**
3. **Fix or fold the "📋 Play Next" label.** `JustFiveMinutes.tsx:262` renders a retired label; the status-cycle grep catches it incidentally. Either fix the label to "Up Next" or add `JustFiveMinutes.tsx` to the status-cycle assertion's surface list. **Flagged 2 weeks running.**
4. **Consider adding the two new decision assertions** (recommender-deleted, enrichment-gates-on-enrichedAt). Both are cheap grep checks and both guard decisions the DECISIONS entries explicitly tag as drift risks.

---

*Audit run autonomously via the `inventory-full-decisions-audit-weekly` scheduled task (Agent B half of `docs/testing-agents-spec.md`), `regress-watch` decisions-audit mode. Primitives: `Bash` grep + code-read only; no live preview. Repo state at run: branch `main`, commit `53b6745`, working tree had untracked files only (prior audit docs, a logo PNG) — audited against working-tree state. No code was modified; no push. Brady decides what to act on.*
