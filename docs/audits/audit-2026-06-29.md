# Decisions Audit — 2026-06-29

**Mode:** `regress-watch` decisions-audit (Agent B, weekly cadence)
**Scope:** 6 of 6 `decision-*` assertions audited. All correspond to LOCKED / committed-direction entries in `docs/DECISIONS.md` (+ `voice-charter.md`). Verification by `Bash` grep / code-read primitives only — no live preview (autonomous run; SKILL.md says don't start a dev server unprompted). Where a live `eval` would add confidence, it's noted.

**Headline:** 6 holds, 0 hard drifts, 0 can't-tells. Two soft flags worth Brady's eye (a retired label on one button; a retired tagline variant on the pile share page), and one assertion whose verification *method* is mis-targeted and should be refined.

---

## Per-assertion verdict

| # | Assertion | Verdict | One-line |
|---|---|---|---|
| 1 | `decision-status-cycle-locked` | ✅ Holds | Internal keys + canonical labels intact; soft flag on a "Play Next" button. |
| 2 | `decision-picker-cta-pick-my-game` | ✅ Holds | "Pick My Game" everywhere, "What Should I Play?" gone, `R` shortcut wired. |
| 3 | `decision-moving-on-canon` | ✅ Holds | Anchor line live; agency-positive affirmation pool; no shame vocab. |
| 4 | `decision-pick-flow-two-inputs` | ✅ Holds | Picker surfaces exactly mood + session length; no genre/platform filter. |
| 5 | `decision-playing-now-cap-3` | ✅ Holds | Cap = 3, enforced at every promotion site — but **not in `store.ts`** (assertion method mis-targeted). |
| 6 | `decision-tagline-canon` | ✅ Holds (named surfaces) | H1/primary/celebration correct; ⚠️ retired "not gonna play itself" variant live on pile share page (off-surface). |

---

### 1. `decision-status-cycle-locked` — ✅ Holds
**Cites:** DECISIONS 2026-04-09 (status cycle lock).
- Internal keys unchanged: `'buried' | 'on-deck' | 'playing' | 'played' | 'bailed'` — `lib/types.ts:3`.
- User-facing labels canonical in `components/TabNav.tsx:22–25`: Backlog / Up Next / Playing Now / Completed. GameCard transition toasts use the same (`GameCard.tsx:439,454,548`).
- **Soft flag (review):** `components/JustFiveMinutes.tsx:262` renders a button labeled **"📋 Play Next"** — `Play Next` is on the retired-labels list (superseded by "Up Next"). The handler correctly triages to `'on-deck'`, so it's a label-only slip, not a state-model drift. Worth a one-word fix to "Up Next" for consistency.
- **Non-issue (noted for the record):** "Cleared" appears in celebration copy (`CompletionCelebration.tsx:171,667`, `StatsPanel.tsx:458`, share cards). These are clearing-the-backlog *flavor verbs* from the authorized `CLEAR_FLAVORS` pool (DECISIONS 2026-05-13), not status labels. The status label is "Completed". No drift.

### 2. `decision-picker-cta-pick-my-game` — ✅ Holds
**Cites:** DECISIONS 2026-05-05 (CTA rename).
- `Pick My Game` present: `Reroll.tsx:449` (aria-label) + `Reroll.tsx:469` (button), `PostImportSummary.tsx:102,143`, `HelpModal.tsx`.
- `What Should I Play` — **zero** user-facing hits across `components/`, `app/`, `lib/`.
- `R` keyboard shortcut wired: `hooks/useKeyboardShortcuts.ts:44` (`e.key === 'r' || 'R'` → `onRoll`), consumed in `app/page.tsx:475–477` as `handleOpenReroll('anything')`.

### 3. `decision-moving-on-canon` — ✅ Holds
**Cites:** `voice-charter.md` (Moved On canon).
- Anchor "Moving on is deciding too." live on landing (`LandingPageV2.tsx:466`).
- Moved-On toast draws from `bailAffirmations` (`GameCard.tsx:531–537`): "You drew a line. That takes guts." / "Knowing when to walk away is a superpower." / "Boundaries set. Pile shrunk. Progress." — all agency-positive.
- Modal copy: "I'm moving on" / "Moving on from this game for good?" (`GameCard.tsx:1289,1296`).
- No shame vocab (`Abandoned`, `Quit on it`, `Dropped`, "gave up") in the moved-on surfaces.

### 4. `decision-pick-flow-two-inputs` — ✅ Holds
**Cites:** DECISIONS / `user-psychology.md` (2-input ceiling, shipped 2026-04-28).
- `components/Reroll.tsx` pre-roll has exactly two user-facing inputs: mood (`moodFilters`, line 52) and session length (`sessionLength`, line 77).
- `platformPreference` (line 88) is read from persistent Settings — not a pick-time input group, doesn't count against the ceiling.
- No genre filter, no platform filter, no separate length filter rendered in the picker.
- *Confidence note:* verified by control inventory in source; a live `eval` counting rendered input groups in the open modal would be the belt-and-suspenders confirmation.

### 5. `decision-playing-now-cap-3` — ✅ Holds (assertion method needs refinement)
**Cites:** DECISIONS 2026-04-06 (Playing Now cap of 3).
- `MAX_PLAYING_NOW = 3` — `lib/constants.ts:59`.
- Enforced before every promotion to `playing`: `GameCard.tsx:424`, `Reroll.tsx:220`, `JustFiveMinutes.tsx:118` all guard `nowPlayingCount >= MAX_PLAYING_NOW` and block + toast.
- **The cap holds**, but the assertion's stated method ("grep `lib/store.ts` for cap logic; if none, hard fail") is **mis-targeted**: enforcement lives at the component call sites, not in the store. A literal reading of the method would have produced a false hard-fail. See follow-ups.

### 6. `decision-tagline-canon` — ✅ Holds on named surfaces; ⚠️ off-surface soft drift
**Cites:** `voice-charter.md` (taglines, landing H1 locked 2026-05-11).
- Primary `get playing.` present across landing, OG cards, about, footers.
- Landing H1 = "Your backlog isn't the problem." / "Deciding is." — `LandingPageV2.tsx:238,241`. Matches canon.
- Celebration "Less shame. More game." present on share pages (`pile/[id]/page.tsx:77`, `clear/[id]/page.tsx:176`).
- Retired "Stop stalling" — **absent**.
- **⚠️ Soft flag (review):** retired tagline variant **"Your backlog's not gonna play itself."** is live on the pile share page — `app/pile/[id]/page.tsx:255`. Voice-charter retired "Your pile's not gonna play itself."; this is the same retired line with pile→backlog. It's *outside* the assertion's scoped surface ("active landing surface" = `LandingPageV2.tsx`), so the assertion technically passes — but a retired tagline is shipped on a recipient-facing share card. Brady's call whether to swap it.

---

## No assertion yet — add one if this should be audited

Committed-direction DECISIONS entries that are code-auditable but have no `decision-*` assertion. Candidates if Brady wants them on the weekly cadence:

- **2026-05-15 — Sort Option B dropped** (no user-facing "How far along are you?" progress field on Playing Now cards). Auditable: grep for absence of a progress-self-report control.
- **2026-05-05 — Similar games strip only on Completed modal** (never Backlog/Up Next/Playing Now/Moved On). Auditable: confirm the gate `status === 'played' && forceExpanded` in `GameCard.tsx`.
- **2026-05-05 — Modal destructive-action disclosure** (Delete + Don't Suggest behind "··· More actions"; Bail stays visible). Auditable: grep `showMoreActions` gating in `GameCard.tsx`.
- **2026-05-13 — Light theme as default** (`lib/store.ts` initial state = `light`). One-line grep assertion.
- **2026-05-01 — GA4 dataLayer init in root layout `<head>`, not consent-gated** (timing-bug regression guard). Auditable: confirm inline init `<script>` in `app/layout.tsx` body-top.
- **2026-06-17 — `/steam-return` passthrough realm route** (custom-scheme 302, `Cache-Control: no-store`). Auditable: grep route shape. *(Low priority — server route, not a user-facing surface; iOS-adjacent.)*

iOS-app decisions (2026-05-20, 2026-06-17 OpenID import) live in the separate `inventoryfull-ios` repo and per the web-repo scope boundary are intentionally **not** audited here.

## Suggested follow-ups (assertion library)

1. **Refine `decision-playing-now-cap-3` method.** Change verification from "grep `lib/store.ts`" to "grep `lib/constants.ts` for `MAX_PLAYING_NOW = 3` **and** confirm a `>= MAX_PLAYING_NOW` guard exists at each promotion call site (`GameCard`, `Reroll`, `JustFiveMinutes`)." The current method would false-hard-fail because enforcement isn't centralized in the store.
2. **Scope decision on `decision-tagline-canon`.** Decide whether the pile share page (`app/pile/[id]/page.tsx`) counts as an audited tagline surface. If yes, the retired "...not gonna play itself." line is a fail and should be swapped. If no, add an explicit exclusion note so future runs don't re-flag it.
3. **Consider a `regress-play-next-label` or fold into status-cycle check.** The "📋 Play Next" button in `JustFiveMinutes.tsx:262` is a retired label slipping into a user-facing surface; the status-cycle grep caught it incidentally. Worth either fixing the label or widening the status-cycle assertion's surface list to include `JustFiveMinutes.tsx`.

---

*Audit run autonomously via the `inventory-full-decisions-audit-weekly` scheduled task (Agent B half of `docs/testing-agents-spec.md`), `regress-watch` decisions-audit mode. Primitives: `Bash` grep + code-read only; no live preview. Repo state at run: branch `main`, commit `cccb71f`, working tree had untracked files only (prior audit docs, `.playwright-mcp/`, a logo PNG) — audited against working-tree state. No code was modified; no push. Brady decides what to act on.*
