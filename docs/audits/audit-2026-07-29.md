# Decisions Audit — 2026-07-29

**Mode:** `regress-watch` decisions-audit (Agent B). Autonomous weekly run.

**Scope:** 6 `decision-*` assertions audited against working-tree code. These encode the LOCKED entries in `docs/DECISIONS.md` that are expressible as a code check. `DECISIONS.md` now runs ~1,810 lines / ~60+ entries; the large majority are locked *direction* (monetization, iOS, Discord bot, infra, one-off bug rationales) with no greppable/renderable assertion — see "No assertion yet" for the audit-worthy subset that could gain one.

**Headline:** 4 hold, 1 drift (unauthorized — **repeat from 07-20, still unfixed**), 1 holds-with-soft-flag, plus one sub-check I can't confirm by grep (the picker `R` shortcut). Working tree clean.

---

## Per-assertion verdict

| Assertion | Cites | Verdict | Evidence |
|---|---|---|---|
| `decision-status-cycle-locked` | DECISIONS 2026-04-09 (L1474) | ❌ **Drift (unauthorized)** | Internal keys intact: `lib/types.ts:3` = `'buried' \| 'on-deck' \| 'playing' \| 'played' \| 'bailed'`. Canon labels intact across `lib/constants.ts:45–49` (Backlog/Up Next/Playing Now/Completed/Moved On) and `TabNav.tsx:22–25`. **But** `components/JustFiveMinutes.tsx:262` still renders a triage button labeled **"📋 Play Next"** whose handler sets `on-deck` (`handleTriage('on-deck')`, L258). "Play Next" is an explicitly **retired** label (voice-charter + this assertion). Same file's own toast (`L134`) correctly says "→ Up Next". **This is the identical drift flagged in the 07-20 audit — not fixed in the intervening week.** |
| `decision-picker-cta-pick-my-game` | DECISIONS 2026-05-05 (L588) | ✅ **Holds** (CTA text) · 🔍 R-shortcut unverified | "Pick My Game" present: Reroll (`Reroll.tsx:471` + `aria-label` L451), PostImport (`PostImportSummary.tsx:107,142`), hero pulse (`app/page.tsx:513`), JSON-LD (`layout.tsx:163`), HelpModal (`HelpModal.tsx:65,69`). "What Should I Play?" absent from all user-facing surfaces (grep clean across `app/ components/ lib/`). **Sub-check I can't confirm:** the assertion also claims "the `R` keyboard shortcut opens the picker with no modal open." No global `keydown` handler for `'r'`/`'R'` exists in source — the only keydown in `Reroll.tsx:394` handles `Escape`/`Tab` (focus trap) *inside the already-open modal*. Either the shortcut was removed, never implemented, or lives somewhere grep didn't reach. Needs a live keypress test (not run — autonomous, no preview server started). |
| `decision-moving-on-canon` | voice-charter.md | ✅ **Holds** | Anchor "Moving on is deciding too." live at `LandingPageV2.tsx:561`. Status label "Moved On" at `lib/constants.ts:49`. GameCard moved-on copy sentiment-positive, no shame: "I'm moving on" (`GameCard.tsx:1360`), "Moving on from this game for good?" (L1367), plus a `bailAffirmations` pool (L533) surfaced on the move-on toast (L550). No retired vocab ("Bailed"/"Dropped"/"Abandoned"/"Quit on it") in user copy — `bailed` / `bailAffirmations` are internal names only. |
| `decision-pick-flow-two-inputs` | DECISIONS 2026-04-28 + user-psychology.md | ✅ **Holds** | `components/Reroll.tsx` surfaces exactly two inputs: mood pills (`moodFilters`, L54) + a session-length drawer (`sessionLength`, L79), collapsed by default. Comment at `L62–63` explicitly cites "the locked 2-input rule." Multi-select mood shelved (`L98`). No genre or platform filter as a default picker input (`platformPreference` is a persisted Setting, not a pre-roll control). |
| `decision-playing-now-cap-3` | DECISIONS 2026-04-06 (L1597) | ✅ **Holds** | `lib/constants.ts:59` = `export const MAX_PLAYING_NOW = 3;`, enforced at **every** entry point that promotes to `playing`: `Reroll.tsx:222`, `JustFiveMinutes.tsx:118`, `GameCard.tsx:426` (all block the 4th promotion with a toast). Matches the entry's "enforced at every entry point." (Cap lives in `constants.ts`, not `store.ts` — same grep-miss noted 07-20, still not a drift.) |
| `decision-tagline-canon` | voice-charter.md (locked 2026-05-11) | ✅ **Holds** (landing) · ⚠️ share-card survivor | Landing H1 exact: "Your backlog isn't the problem." (`LandingPageV2.tsx:429`) / "Deciding is." (`L432`). Primary "get playing." present (layout metadata `L41,70,81`; landing chrome `LandingPageV2.tsx:126–127`; Wordmark). "Less shame. More game." on both OG landings (`app/pile/[id]/page.tsx:77`, `app/clear/[id]/page.tsx:56,176`). "Stop stalling" absent from active `app/`+`components/`. **Soft:** `app/pile/[id]/page.tsx:255` renders "Your backlog's not gonna play itself." — a reworded (pile→backlog) survivor of the retired "Your pile's not gonna play itself." family. Not the exact retired string and not on the landing surface, so the assertion's literal check passes — but it's on a share-card surface. **Repeat soft flag, 3rd week running (07-19, 07-20, now).** |

**Tally:** ✅ 4 · ❌ 1 · ⚠️ 1 soft · 🔍 1 sub-check unconfirmed.

---

## The one hard drift, in detail (repeat, unfixed)

**`components/JustFiveMinutes.tsx:262` — "📋 Play Next" button uses a retired status label.**

- The button triages a game to `on-deck` (`handleTriage('on-deck')`, L258). The locked canon label for `on-deck` is **"Up Next"** — used everywhere else, including `constants.ts:46`, `TabNav.tsx:23`, and this file's *own* success toast at `L134` ("→ Up Next").
- "Play Next" is on the voice-charter retired list ("do not reintroduce: … Play Next, On Deck, …"). Exactly the terminology drift the status-cycle lock exists to prevent.
- **Severity: low, but now persistent.** One button label inside the "Just Five Minutes" triage flow (not a core-loop surface), functionally correct (sets the right state). But it was flagged in the **07-20 audit** with the same line/fix and remains unaddressed a week later — the drift has now survived one full audit cycle. No superseding DECISIONS entry authorizes it → stays ❌, not ⚠️.
- **Suggested fix (Brady's call — not applied):** change `JustFiveMinutes.tsx:262` from `📋 Play Next` to `🎯 Up Next`, matching the TabNav icon+label and the file's own toast.

*(The other `Play Next` hits — `GameCard.tsx:362`, `lib/seedData.ts:39` — are code comments, not user-facing. No action.)*

---

## Carried-over soft flag (3rd week)

`app/pile/[id]/page.tsx:255` — **"Your backlog's not gonna play itself."** on the pile share card. Flagged 07-19 and 07-20; still present. Verdict unchanged: **real, low-priority.** Reworded descendant of the retired subhead — retirement targeted the *sentence*; swapping "pile"→"backlog" kept it alive. Not on the landing (strictest lock), so no hard fail. Brady's call whether the pile card adopts the current voice ("Your backlog isn't the problem. Deciding is." or a share-appropriate line) or the survivor is deliberate. Three weeks unactioned suggests either intentional or genuinely low-priority — worth a one-line decision to stop re-flagging it.

---

## Suggested follow-ups (assertions to add / refine)

**Refine:**
- `decision-status-cycle-locked` — caught the drift correctly, but add `JustFiveMinutes.tsx` to its explicit surface list (currently names GameCard/GridCard/types/store; the triage flow is a real status-writing surface it doesn't enumerate). Recommended a week ago; repeating.
- `decision-picker-cta-pick-my-game` — the `R`-shortcut sub-check can't be answered by grep and wasn't testable this run. Either (a) demote it to a live-only check the weekly grep audit skips, or (b) if the shortcut was intentionally dropped, remove the sub-check and the "(or press R)" note in `assertions.md`'s Pages-of-record table (L31). Right now it's an unverifiable clause riding on an otherwise-clean assertion.
- `decision-tagline-canon` — the literal check has missed the reworded pile-card survivor **three weeks straight**. Broaden the share-card check to a fuzzy match on the retired sentence stem ("not gonna play itself") across the surfaces the assertion already claims (share cards, footers), or explicitly whitelist the pile line so it stops recurring as noise.

**No assertion yet — add one if it should be audited** (LOCKED, code-checkable, no `decision-*` today):
- **2026-05-20 — imports default to Backlog / no auto-status (LOCKED, user-agency; reinforced in the 2026-05-13 LOCKED entry at DECISIONS L1783).** Highest-value gap. *Recurring* violation pattern (Xbox achievements, Steam HLTB inference — both rolled back). Cheaply greppable: import mappers must set `status: 'buried'`, never `playing`/`played`/`on-deck` from data signals. Strongest candidate — flagged prior weeks, still absent.
- **2026-07-19 — HLTB scraper retired; length via RAWG `playtimeHours`.** Recent + checkable: `app/api/hltb/route.ts` deleted, single helper `gameLengthHours()` reads `playtimeHours ?? hltbMain`, no *new* writes to `hltbMain`. Worth a regression check given it shipped this month and the follow-up list carried a hard pre-deploy DB-migration gate.
- **2026-07-15 — GA4 removed / cookieless (no CookieBanner).** Checkable by absence: `components/CookieBanner.tsx` deleted, no `gtag`/`dataLayer` init in `app/layout.tsx`, `lib/analytics.ts` is a Vercel stub.
- **2026-06-30 — completion-recommender deleted.** Checkable by absence of `lib/recommendations.ts` and any `getCompletionRecommendations`/`getWishlistRecommendations` import.
- **2026-06-30 — status-event log shipped (`lib/statusEvents.ts`).** Checkable: file exists, wired into store status-write sites, local-only (no Supabase mirror).
- **2026-05-05 — similar games ("From your shelf") fires on Completed modal only.** Greppable: gated to `status === 'played'` in `CompletionCelebration.tsx`, never on Moved On.

**Too abstract to encode (leave to other skills):** 2026-04-26 "Enjoy your games again" mission, anti-gamification posture, moat-is-decision-quality — belong to `feature-creep-audit` / `psychology-redteam`, per the assertion-library's own guidance ("if a decision can't be expressed as a check, don't add a `decision-*`").

---

*Audit run: `regress-watch` decisions-audit mode (Agent B), autonomous weekly. Working tree at commit `848aa78` (`848aa782e2d6c5621aef4f84a873c141b211bde5`), clean — no uncommitted changes. Grep-primitive checks only; no preview server started, no code modified, no push. Drift is surfaced for Brady's decision, not auto-fixed.*
