# Decisions Audit — 2026-07-13

**Mode:** `regress-watch` decisions-audit (Agent B, weekly cadence)
**Scope:** 6 `decision-*` assertions checked against working-tree code. DECISIONS.md carries 89 entries; the assertion library encodes 6 of the LOCKED ones. All 6 assertions ran.
**Verdict headline:** 5 hold · 1 drift (minor, unauthorized) · 0 can't-tell. Plus 2 low-severity follow-up notes.

_(Repo had uncommitted changes at time of audit: `docs/INDEX.md`, `docs/session-resume-2026-07-02.md` modified; several prior audit docs + one PNG untracked. Audit ran against working-tree state.)_

---

## Per-assertion verdict

### `decision-status-cycle-locked` — ⚠️ Drift (unauthorized, minor)
- ✅ Internal status keys intact in `lib/types.ts`: `buried | on-deck | playing | played | bailed`.
- ✅ No retired **status labels** leaking as user-facing status names. `"Cleared"` appears only as celebration **verb** copy (`CompletionCelebration.tsx`, `StatsPanel.tsx`) — sanctioned by the 2026-05-13 DONE_FLAVORS decision, not used as the Completed status label. `"Your Queue"` in `lib/store.ts:418,424` is legacy category-migration mapping (old → `The Pile`), not a live label.
- ❌ **One retired label leak:** `components/JustFiveMinutes.tsx:262` renders a triage button `📋 Play Next` that promotes a game to `on-deck`. The locked label for `on-deck` is **Up Next**; `Play Next` was explicitly retired 2026-04-09. Everywhere else (`GameCard.tsx`, `page.tsx`, `TabNav`) correctly uses "Up Next". No DECISIONS entry authorizes reviving "Play Next" — this is a genuine terminology regression, isolated to one button in the Just-Five-Minutes triage flow. Low blast radius, but it's exactly the drift this assertion exists to catch.

### `decision-picker-cta-pick-my-game` — ✅ Holds
- ✅ Zero occurrences of `What Should I Play` in `app/`, `components/`, `lib/`.
- ✅ `Pick My Game` present in `app/layout.tsx` (JSON-LD), `app/page.tsx` (hero), `components/PostImportSummary.tsx`, `components/Reroll.tsx`, `components/HelpModal.tsx` (and `app/privacy/page.tsx`).
- 🔍 *Not verified this run:* the live `R`-keypress-opens-picker behavior (requires a running preview; autonomous run skipped the dev server per skill guidance). Grep-level check passes.

### `decision-moving-on-canon` — ✅ Holds
- ✅ Anchor sentence present verbatim: `Moving on is deciding too.` at `components/LandingPageV2.tsx:466`.
- ✅ `components/GameCard.tsx` frames the exit as agency, not shame: `"I'm moving on"` (1357), `"Moving on from this game for good?"` (1364). `CompletionCelebration.tsx` uses "Moving on." in done-flavor copy.
- ✅ No retired bail vocab in user-facing strings. `setBailed` / `unBail` (`GameCard.tsx:380,547`) are internal function names, not copy — correctly out of scope.

### `decision-pick-flow-two-inputs` — ✅ Holds
- ✅ `components/Reroll.tsx` default pre-roll surfaces two inputs (mood + session length); genre/platform/length filters are collapsed into drawers per the 2026-04-28 locked rule (`showVibes`, session-length drawer collapsed by default, comments at lines 58–68). `sessionLength` is the live second axis; no genre/platform filter rendered at default.
- 🔍 *Follow-up (comment rot, not functional drift):* `Reroll.tsx:58` comment still reads "modal opens at 2 CTAs + **energy pills**" — stale reference to the retired *energy* axis (superseded by session length, 2026-04-28). Code uses `sessionLength`; only the comment lags. Worth a one-line fix so future readers don't think energy is still live.

### `decision-playing-now-cap-3` — ✅ Holds (strongly)
- ✅ `MAX_PLAYING_NOW = 3` in `lib/constants.ts:59`, enforced at **every** promotion site: `GameCard.tsx:425,566`, `Reroll.tsx:221`, `JustFiveMinutes.tsx:118`, `app/page.tsx:317`. UI also shows the cap notice at `app/page.tsx:899`. Cap is real enforcement (blocks the 4th promotion with a toast), not copy-only.

### `decision-tagline-canon` — ✅ Holds
- ✅ Active landing is `components/LandingPageV2.tsx` (imported by `app/page.tsx:29`). H1/subhead "Your backlog isn't the problem. Deciding is." present there.
- ✅ Primary tagline `get playing.` present across OG routes, `Wordmark.tsx`, `about`, `LandingPageV2`, `NinetiesMode`.
- ✅ Retired `Stop stalling` — zero occurrences. Retired `Your pile's not gonna play itself.` — zero occurrences on the active landing surface.
- 🔍 *Follow-up (out of assertion scope, worth an eye):* `app/pile/[id]/page.tsx:255` renders `Your backlog's not gonna play itself.` — a close cousin of the retired `Your pile's not gonna play itself.` line, living on the **pile share page** (not the landing, so not a hard fail under this assertion's scope). If the retirement was meant to kill the sentiment app-wide and not just the exact "pile" phrasing, this share-page line should be revisited.

---

## Suggested follow-ups

**Drift to act on (Brady's call):**
1. `JustFiveMinutes.tsx:262` — change `📋 Play Next` → `📋 Up Next` to match the locked status cycle. Only retired-label leak found this run.

**Copy/comment hygiene:**
2. `Reroll.tsx:58` — update stale "energy pills" comment to "session-length pills" (energy axis retired 2026-04-28).
3. `app/pile/[id]/page.tsx:255` — decide whether "Your backlog's not gonna play itself." on the pile share card is intentional or a survivor of the retired tagline family; update or bless it.

**Assertions to consider adding (LOCKED entries with no `decision-*` coverage):**
- **No-auto-assign-status / user-agency rule** (DECISIONS.md line ~1640, marked **LOCKED**; also AGENTS.md locked decision #7). Third-time-caught regression pattern (Xbox achievements, Steam HLTB inference). This is the single most-violated locked rule and has *no* `decision-*` assertion. Candidate check: grep import/enrichment paths for any write of `status: 'played' | 'on-deck' | 'bailed'` derived from external data signals rather than explicit user action. High value — recommend adding.
- **Mission "Enjoy your games again"** (2026-04-26, locked) — abstract, probably belongs to `psychology-redteam` not a grep assertion. No action.
- **Sort Option B dropped / no progress-self-report field** (2026-05-15, locked) — could be a cheap grep assertion (no "How far along are you?" field on Playing Now cards). Low priority.
- **OG runtime split** (2026-04-21) — already covered by `og-*` / `regress-*` assertions; no `decision-*` needed.

**Assertions to retire/refine:** none. All 6 remain load-bearing and matched a real surface.

---

_Audit run: `regress-watch` decisions-audit mode, autonomous weekly schedule. Working tree at commit `16060004fa143c94b31dd9e21c4f193b2ec39d21` (uncommitted doc changes present, noted above). Grep-primitive checks only; live-preview checks (R-keypress, vision) skipped per autonomous-run guidance. No code modified; no push._
