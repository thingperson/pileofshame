# Decisions Audit — 2026-06-15

**Mode:** `regress-watch` decisions-audit (Agent B, autonomous weekly run)
**Scope:** 6 `decision-*` assertions checked against working-tree code. DECISIONS.md carries 78+ dated decision entries (newest 2026-05-20 iOS app); the 6 assertions encode the LOCKED entries deemed code-auditable. Locked-but-unasserted entries listed at the bottom. No new DECISIONS entries since the 2026-06-08 audit, so the assertion set is unchanged.

**Headline:** 4 holds, 1 new drift (unauthorized), 1 hold-with-soft-watch. **Last week's one real drift — the "What Should I Play?" → "Pick My Game" copy sweep — is now RESOLVED** in the working tree. One new status-label drift surfaced in `JustFiveMinutes.tsx` that prior audits missed.

> **Note:** repo had uncommitted changes at time of audit. The working tree contains in-progress edits to `components/HelpModal.tsx`, `components/PostImportSummary.tsx`, `app/page.tsx`, `app/privacy/page.tsx`, `.claude/skills/regress-watch/assertions.md`, and an archival rename `components/LandingPageClassic.tsx → notes/_archive/LandingPageClassic.tsx`. These are Brady acting on last week's follow-ups (not yet committed). Audit ran against working-tree state. No code modified; no push.

---

## Per-assertion verdict

### ❌ `decision-status-cycle-locked` — Drift (unauthorized)
Cites DECISIONS 2026-04-09 (status cycle locked: Backlog → Up Next → Playing Now → Completed, Moved On sibling).
- **Internal keys intact:** `lib/types.ts:3` → `'buried' | 'on-deck' | 'playing' | 'played' | 'bailed'`. Unchanged.
- **Canonical labels intact in the primary surfaces:** `lib/constants.ts:46` maps `on-deck` → `label: 'Up Next'`. GameCard/GridCard pills use the constants. ✅
- **New drift — retired label as a live button:** `components/JustFiveMinutes.tsx:262` renders a user-facing triage button reading **"📋 Play Next"** wired to `handleTriage('on-deck')`. "Play Next" is an **explicitly retired label** (voice-charter.md terminology table + this assertion's retired list). The `on-deck` status must read **"Up Next"**. The sibling buttons in the same group are also off-canon-adjacent: "🔥 Playing this now" (→`playing`, canonical "Playing Now"), "📚 Back to The Pile" (→`pile`; "Pile" is an allowed casual synonym, fine). The clear failure is "Play Next."
  - Provenance: file last touched `b5569de` (2026-05-08, glass-token migration); the label predates that. Longstanding miss — prior status-cycle checks grepped GameCard/GridCard surfaces and didn't reach `JustFiveMinutes.tsx`. Unauthorized: no DECISIONS entry preserves "Play Next."
  - **Recommend:** change `JustFiveMinutes.tsx:262` "📋 Play Next" → "📋 Up Next". One-line fix.
- **Soft watch (carryover, not a fail):** `components/StatsPanel.tsx:458` renders `<StatCard label="Cleared" …>`. "Cleared" is the retired label for *Completed*, used here as a metric name, not a status pill. Borderline off-terminology. Consider "Completed."

### ✅ `decision-picker-cta-pick-my-game` — Holds (last week's drift resolved)
Cites DECISIONS 2026-05-05 (CTA renamed "What Should I Play?" → "Pick My Game").
- **Primary CTA holds:** "Pick My Game" appears 14× across `app/` + `components/` (hero, post-import, Reroll header + `aria-label`, JSON-LD). 🎲 glyph present.
- **Second clause now passes:** `What Should I Play?` returns **0 user-facing occurrences** (grep across `app/`, `components/`). The 2026-06-08 follow-up — sweep the stale references in `HelpModal.tsx` (4 hits) and `PostImportSummary.tsx:102` — has been executed in the working tree (those files are git-modified, plus `app/privacy/page.tsx`). The button and surrounding copy now agree.

### ✅ `decision-moving-on-canon` — Holds
Cites voice-charter.md ("Moving on is deciding too"; sentiment locked, variations OK).
- Anchor present: `LandingPageV2.tsx:466` — "Moving on is deciding too."
- In-app affirmation pool intact: `GameCard.tsx:531–537` ("You drew a line. That takes guts." / "Knowing when to walk away is a superpower." / …). Button "I'm moving on" (`:1289`). All sentiment-positive.
- No retired user-facing vocab (Bailed/Dropped/Abandoned/Quit on it); `bailed`/`bailAffirmations` are internal identifiers only.

### ✅ `decision-pick-flow-two-inputs` — Holds
Cites DECISIONS 2026-04-28 + user-psychology.md (2-input ceiling: mood + session length).
- `components/Reroll.tsx`: two axes present — mood (`ALL_MOODS`/`toggleMood`, `:21,114`) and session length (`SessionLength`/`sessionLength`/`showSessionLengthDrawer`, `:12,76,91`). Telemetry carries `session_length` (`:192`).
- **No prohibited third axis at pick-time.** `platformPreference` (`:102`) is a persisted *setting* read from the store and passed into `getEligibleGames` as a background filter — it is **not** a pick-flow input group surfaced in the pre-roll modal. The `genres` references (`:165,272`) are the picked game's own genres for cooldown/display, not a genre filter control. Cap holds.
- Cosmetic carryover: comment at `Reroll.tsx:71` still says "energy pills" — stale pre-pivot wording (DECISIONS 2026-04-27 PM). Harmless.

### ✅ `decision-playing-now-cap-3` — Holds (strong)
Cites DECISIONS 2026-04-06 (Playing Now cap = 3, every entry point).
- `lib/constants.ts:59` → `export const MAX_PLAYING_NOW = 3;`
- Enforced at every promotion entry point: `components/Reroll.tsx:234–235`, `components/JustFiveMinutes.tsx:118–119`, `components/GameCard.tsx:424–425`. Each blocks the 4th with the toast "Playing Now is capped at 3. Finish or shelve something first."

### ✅ `decision-tagline-canon` — Holds (one soft watch)
Cites voice-charter.md (primary `get playing.`; landing H1 "Your backlog isn't the problem. Deciding is."; "Stop stalling" + "Your pile's not gonna play itself." retired).
- **Primary tagline holds:** `get playing.` (lowercase, period) present 20× across titles/OG/footers.
- **Active landing H1 holds:** `app/page.tsx:30` imports `LandingPageV2`; hero reads "Your backlog isn&apos;t the problem." / "Deciding is." (`LandingPageV2.tsx:238,241`) — matches voice-charter 2026-05-11. *(The straight-apostrophe grep missed it initially; the source uses `&apos;`.)*
- **Retired literals absent:** "Stop stalling" and "Your pile's not gonna play itself." — neither found.
- **Stale-assertion follow-up addressed:** last week's #1 (point the assertion at `LandingPageV2`, archive `LandingPageClassic`) is reflected — `LandingPageClassic.tsx` is being archived to `notes/_archive/` in the working tree, and `assertions.md` already cites the V2 surface + the 2026-05-11 H1.
- **Soft watch (new):** `app/pile/[id]/page.tsx:255` CTA reads **"Your backlog's not gonna play itself."** — a reworded version of the retired "Your pile's not gonna play itself." subhead sentiment (pile→backlog swap). It is **not a literal match** to the banned string, and it lives on a secondary share-card landing surface, so the assertion as written holds. But the assertion's surface list explicitly includes "share cards," and voice-charter put this sentiment in the retired column. Worth Brady's eye: either bless this reworded variant or replace with the canonical "Your backlog isn't the problem. Deciding is." framing.

---

## Suggested follow-ups

**Code to sweep (the one real drift):**
1. `JustFiveMinutes.tsx:262` — change "📋 Play Next" → "📋 Up Next". Retired status label appearing as a live triage button; canonical `on-deck` label is "Up Next" (`constants.ts:46`). One-line fix.

**Soft / Brady's call:**
2. `app/pile/[id]/page.tsx:255` — "Your backlog's not gonna play itself." is a reworded retired tagline on the pile share-card CTA. Bless it or swap to canonical framing.
3. `StatsPanel.tsx:458` — "Cleared" stat label → "Completed" for status-cycle terminology consistency (carryover from 2026-06-08).
4. `Reroll.tsx:71` — comment "energy pills" is stale pre-pivot wording; tidy when next in the file (carryover).

**Assertions to refine:**
5. `decision-status-cycle-locked` — broaden the grep surface to include `components/JustFiveMinutes.tsx` (and any other triage/quick-action component) explicitly. This audit only caught "Play Next" because the grep widened to all of `components/`; the prior catalog implicitly leaned on GameCard/GridCard. Add JustFiveMinutes to the named surfaces.

**No assertion yet — add one if this should be audited** *(unchanged from 2026-06-08; all grep-auditable except where noted):*
- 2026-05-05 — "From your shelf" similar games fires only on Completed modal (`GameCard.tsx`, gated `status === 'played' && forceExpanded`).
- 2026-05-05 — Modal destructive-action disclosure (Delete/Don't Suggest behind "More actions"; Bail always visible).
- 2026-05-13 — `DONE_FLAVORS` separate share-copy pool for `isNonFinishable` games (`CompletionCelebration.tsx`).
- 2026-05-05 — `navigator.share` archetype payload `{title, url}` only, no `text` (`ArchetypeCard.tsx`).
- 2026-05-15 — Sort Option B killed (no user progress self-report field on Playing Now cards; absence check).
- 2026-04-26 — North Star + AGENTS.md #7 (never auto-assign status from data signals). Too abstract/cross-surface for a `decision-*` — belongs in `psychology-redteam` / `feature-creep-audit`. Listed for completeness, not recommended.
- 2026-05-20 — iOS app decisions (separate repo `inventoryfull-ios`). Out of scope for this repo's code audit.

---

*Audit run: `regress-watch` decisions-audit mode, autonomous weekly cadence. Working tree at commit `1ae1678` (repo had uncommitted changes at audit time — see note above; same HEAD SHA as the 2026-06-08 run, the deltas are unstaged working-tree edits). Cheapest-primitive-first (grep > read); no preview server started. No code modified; no push. Brady decides what to act on.*
