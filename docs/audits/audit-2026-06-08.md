# Decisions Audit — 2026-06-08

**Mode:** `regress-watch` decisions-audit (Agent B, autonomous weekly run)
**Scope:** 6 `decision-*` assertions checked against working-tree code. DECISIONS.md carries 78 dated decision entries; the 6 assertions encode the LOCKED entries deemed code-auditable. Locked-but-unasserted entries are listed at the bottom.

**Headline:** 4 holds, 1 drift (unauthorized), 1 drift (authorized — stale assertion). 0 can't-tells. No hard regressions of the kind Brady catches on review, but two assertion-vs-code gaps worth a glance.

---

## Per-assertion verdict

### ✅ `decision-status-cycle-locked` — Holds
Cites DECISIONS 2026-04-09 (status cycle locked) + 2026-04-09 ("bail" retired from user copy).
- Internal keys intact: `lib/types.ts:3` → `'buried' | 'on-deck' | 'playing' | 'played' | 'bailed'`. Unchanged.
- User-facing labels all present and correctly cased across `components/`: Backlog ×48, Up Next ×17, Playing Now ×22, Completed ×20, Moved On ×6.
- No retired status labels reintroduced as status pills. `"bailed"` appears only as the internal status key (e.g. `GameCard.tsx:529,546,558`), never as user-facing copy.
- **Soft watch (not a fail):** `components/StatsPanel.tsx:458` renders a stat card `label="Cleared"`. "Cleared" is the retired label for the *Completed* status (voice-and-tone.md terminology table). It's being used here as a metric name ("Cleared" count), not a status pill — borderline off-terminology, not a status-cycle regression. Consider renaming to "Completed" for consistency.

### ❌ `decision-picker-cta-pick-my-game` — Drift (unauthorized)
Cites DECISIONS 2026-05-05 (CTA renamed "What Should I Play?" → "Pick My Game").
- **Primary CTA holds:** "Pick My Game" is the live button text — `app/page.tsx:740`, `components/PostImportSummary.tsx:143`, `components/Reroll.tsx:483` (+ `aria-label` at `:463`), JSON-LD at `app/layout.tsx:180`. The `🎲` glyph is present.
- **But the assertion's second clause fails:** "old label `What Should I Play?` does not appear in any user-facing context." It still appears user-facing in:
  - `components/HelpModal.tsx:65` (section title), `:69` (Q&A question), `:47` & `:116` (answer body) — the Help modal still teaches the old name.
  - `components/PostImportSummary.tsx:102` — "Hit "What Should I Play?" when you're ready." This is a direct inconsistency: the button right above it (`:143`) now says "Pick My Game."
  - `app/privacy/page.tsx:50,280` — feature described as the "What Should I Play?" feature.
- voice-and-tone.md terminology table puts "What Should I Play?" in the NOT column, so these are off-canon, not authorized. No DECISIONS entry preserves the old label anywhere user-facing.
- **Verdict:** the rename shipped on the button but the supporting copy was never swept. Recommend updating HelpModal + PostImportSummary:102 to "Pick My Game" (privacy page is lower-stakes descriptive text, Brady's call). `app/page.tsx:748` is a code comment — ignore.

### ✅ `decision-moving-on-canon` — Holds (assertion-surface refinement suggested)
Cites voice-charter.md ("Moving on is deciding too" anchor; sentiment locked, variations OK).
- The in-app Moved On flow in `GameCard.tsx` is agency-affirming and shame-free. Confirmation copy "Moving on from this game for good?" (`:1296`), button "I'm moving on" (`:1289`) / "Yeah, not for me" (`:1306`), and a randomized affirmation pool (`:531–537`): *"You drew a line. That takes guts." / "Knowing when to walk away is a superpower." / "One less thing weighing on you. Nice." / "Boundaries set. Pile shrunk. Progress." / "That decision? Already worth more than 10 more hours sunk."* All sentiment-positive, no failure/regret/shame framing.
- No retired user-facing vocab (Bailed/Dropped/Abandoned/Quit on it) — `bailed`/`setBailed` are internal identifiers only.
- The literal anchor "Moving on is deciding too" lives on the landing components (`LandingPageV2.tsx:466`, `LandingPageClassic.tsx:271`), **not** in the assertion's named surfaces (GameCard/Reroll/CompletionCelebration). Sentiment is satisfied by the affirmation pool, so this holds — but the assertion over-specifies the surface. See follow-ups.

### ✅ `decision-pick-flow-two-inputs` — Holds
Cites DECISIONS 2026-04-28 + user-psychology.md (2-input ceiling: mood + session length).
- `components/Reroll.tsx` pre-roll (render at `:493–559`) surfaces: two entry CTAs ("🎲 Anything" / "⏱️ 5-min tryout") and a collapsed **Session length** drawer (Small/Medium/Large, copy locked per psychology §3, `:539–542`). Mood selection exists via `moodFilters`/`toggleMood`/`ALL_MOODS` (`:21,66,114`), one tap away.
- **No prohibited third axis:** no genre filter, no platform filter, no separate length filter visible at default. The two input axes remain mood + session-length. Multi-select mood was deliberately shelved (`:110–111`, Apr 14). Cap holds.
- Minor: comment at `:71` still says "energy pills" — stale wording from the pre-pivot energy framing (DECISIONS 2026-04-27 PM). Cosmetic, not drift.

### ✅ `decision-playing-now-cap-3` — Holds (strong)
Cites DECISIONS 2026-04-06 (Playing Now cap = 3, enforced at every entry point).
- `lib/constants.ts:59` → `export const MAX_PLAYING_NOW = 3;`
- Enforced at every promotion entry point, matching the "every entry point" language of the decision: `components/Reroll.tsx:232–235`, `components/JustFiveMinutes.tsx:119`, `components/GameCard.tsx:425` and `:566`. Each blocks the 4th promotion with the toast "Playing Now is capped at 3. Finish or shelve something first."

### ⚠️ `decision-tagline-canon` — Drift (authorized — assertion is stale)
Cites voice-charter.md (primary tagline `get playing.`; landing subhead; "Stop stalling" retired).
- **Primary tagline holds:** `get playing.` (lowercase, period) present in titles, OG metadata, footers — `app/layout.tsx:42,71,82,88`, `app/opengraph-image.tsx:5,118`, `app/pile/[id]/...`, `app/about/page.tsx:59`.
- **Retired line absent:** "Stop stalling" not found anywhere user-facing. ✅
- **Stale assertion text:** the assertion says the landing subhead is *"Your pile's not gonna play itself."* That line lives in `LandingPageClassic.tsx:164` — **which is not the active landing component.** `app/page.tsx:30` imports `LandingPageV2`, whose hero reads *"Your backlog isn't the problem. Deciding is."* (`LandingPageV2.tsx:241`). This matches voice-charter.md's 2026-05-11 update ("H1 is now 'Your backlog isn't the problem. Deciding is.'"). The code is correct; the **assertion** is out of date. Authorized drift — update the assertion, not the code.

---

## Suggested follow-ups

**Assertions to refine:**
1. `decision-tagline-canon` — update the cited landing subhead from "Your pile's not gonna play itself." to "Your backlog isn't the problem. Deciding is." (voice-charter.md 2026-05-11). Point the surface at `LandingPageV2.tsx`, the active component. `LandingPageClassic.tsx` appears dormant — worth confirming it's intentionally retained and not dead code.
2. `decision-moving-on-canon` — broaden the "Check" to accept an equivalent agency-affirming affirmation pool (the `bailAffirmations` array), not only the literal anchor string. The anchor lives on landing; the in-app moved-on surface satisfies the *sentiment* via the pool. As written, a literal-string grep on GameCard/Reroll/CompletionCelebration would false-fail.

**Code to sweep (the one real drift):**
3. `decision-picker-cta-pick-my-game` — sweep stale "What Should I Play?" references in `HelpModal.tsx` (4 hits) and `PostImportSummary.tsx:102` to "Pick My Game." The button renamed; the surrounding copy didn't follow. PostImportSummary is the sharpest inconsistency (button and instruction text disagree on the same screen).

**Minor / cosmetic:**
4. `StatsPanel.tsx:458` "Cleared" stat label → "Completed" for status-cycle terminology consistency (soft).
5. `Reroll.tsx:71` comment says "energy pills" — stale pre-pivot wording; harmless, tidy when next in the file.

**No assertion yet — add one if this should be audited:**
These LOCKED/committed-direction DECISIONS entries have no `decision-*` assertion. Several are grep-auditable and would strengthen the catalog:
- 2026-05-05 — Similar games ("From your shelf") fires only on the Completed modal (`GameCard.tsx`, gated `status === 'played' && forceExpanded`). Grep-auditable.
- 2026-05-05 — Modal destructive-action disclosure (Delete/Don't Suggest behind "More actions"; Bail always visible). Grep/eval-auditable.
- 2026-05-13 — DONE_FLAVORS separate share-copy pool for `isNonFinishable` games (`CompletionCelebration.tsx`). Grep-auditable.
- 2026-05-05 — `navigator.share` archetype payload passes `{title, url}` only, no `text` (`ArchetypeCard.tsx:33`). Grep-auditable.
- 2026-05-15 — Sort Option B killed (no user progress self-report field on Playing Now cards). Auditable as an absence check.
- 2026-04-26 — North Star "Enjoy your games again" and AGENTS.md #7 (never auto-assign status from data signals). **Too abstract / cross-surface for a `decision-*`** — belongs in `psychology-redteam` / `feature-creep-audit`, per the skill's own guidance. Listed for completeness, not recommended as an assertion.
- 2026-05-20 — iOS app decisions (separate repo `inventoryfull-ios`). Out of scope for this repo's code audit.

---

*Audit run: `regress-watch` decisions-audit mode, autonomous weekly cadence. Working tree at commit `1ae1678` (repo had untracked files at audit time — `docs/audits/audit-2026-05-25.md`, `public/if-logos/inventory-full-logo-1024-square.png`; no tracked modifications). No code was modified; no push. Cheapest-primitive-first (grep > read); no preview server started. Brady decides what to act on.*
