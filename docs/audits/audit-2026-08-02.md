# Regress Watch — 2026-08-02

**Mode:** `regress-watch` **default** (named-bug catalog + surface + process assertions). Not the decisions-audit — Monday's `inventory-full-decisions-audit-weekly` owns that.

**Scope:** light pass. Exactly **one user-facing change** in the last 8 days (`components/JustFiveMinutes.tsx`, a copy fix). Everything else was docs, `.claude/` config, and the iCloud path sweep. Wordmark / header / tabnav / OG / theme surfaces were untouched, so their `regress-*` assertions are out of scope this week rather than skipped.

**Headline:** clean. The 07-29 hard drift is **fixed**, the 3-week share-card soft flag is **whitelisted and closed**, and the R-shortcut sub-check is now **verifiable and passing**. One infra fix applied outside the repo (below), one stale assertion-hygiene item.

---

## Per-assertion verdict

| Assertion | Tier | Verdict | Evidence |
|---|---|---|---|
| `decision-status-cycle-locked` | grep | ✅ **PASS** *(was ❌ 07-20 + 07-29)* | `components/JustFiveMinutes.tsx:262` now reads `🎯 Up Next` — exact match to `lib/constants.ts:46` and `components/TabNav.tsx:23`, icon included. Internal keys intact (`lib/types.ts:3`). Remaining `Play Next` hits are code comments only (`lib/seedData.ts:39`, `GameCard.tsx:362`) — ruled no-action 07-29, not re-flagged. |
| `decision-picker-cta-pick-my-game` | grep | ✅ **PASS** *(sub-check was 🔍 07-29)* | `What Should I Play` absent from `app/ components/ lib/`. R shortcut now confirmed in source: `hooks/useKeyboardShortcuts.ts:44-48` binds `'r'`/`'R'` → `onRoll()`, correctly guarded by modal-open (`L33`) and editing-context (`L42`) checks. The 07-29 "unverifiable" verdict was an artifact of grepping `app/`+`components/`; assertion fix `ebf27d7` repointed it at `hooks/`. |
| `decision-tagline-canon` | grep | ✅ **PASS** — soft flag **closed** | `app/pile/[id]/page.tsx` share-card subhead is now explicitly whitelisted (`assertions.md:193`, Brady's 07-30 ruling). Soft-flagged 07-19 / 07-20 / 07-29; this is the first run where it correctly resolves as not-a-violation. Noise loop closed. |
| `og-no-webp-img` | grep | ✅ **PASS** | No `.webp` references in any `app/**/opengraph-image.tsx`. |
| `process-read-the-asset` | process | ✅ observed | No asset described this run without reading it. |
| `process-trust-computed-style-on-mismatch` | process | n/a | No vision/inspect mismatch — vision tier not run. |
| `regress-wordmark-*`, `regress-header-*`, `tabnav-sprites-render`, `regress-tabnav-muted-teal-v2.1`, `regress-void-theme-aa`, `og-pile-renders`, `og-clear-renders` | vision/eval | ⏭️ **out of scope** | No commit this week touched these surfaces. Also not runnable: no preview server up, and SKILL.md Step 1 says ask before starting one — autonomous run, so none started. |

**Tally:** ✅ 4 checked + 2 process · ❌ 0 · ⚠️ 0 · ⏭️ 7 out of scope.

Working tree clean at `a1dbe6a`. No code modified, nothing pushed.

---

## 🔧 Path sweep — round 2 (applied)

The 07-30 sweep (`abfce39`) covered **git-tracked, in-repo** paths. Everything below was outside that set and still pointed at the dead `~/Desktop/getplaying`. All fixed this run; `.bak-20260802` copies left beside each edited config.

| What | Why it mattered | Status |
|---|---|---|
| **Agent memory dir** — `~/.claude/projects/-Users-bradywhitteker-Desktop-getplaying/memory/` | **Biggest one.** Memory is keyed by cwd path. All 30 memories (canon pointers, feedback, session context) were stranded under the old key. `-Users-bradywhitteker-dev-getplaying` already existed with 2 sessions — meaning **you've already run two sessions from the new path with zero memory loaded.** | ✅ copied to the dev-keyed dir; 30 entries verified |
| `~/.claude/scheduled-tasks/inventory-full-regress-watch-weekly/SKILL.md:8` | This task. Landed in the empty husk, found no repo. | ✅ fixed |
| `~/.claude/scheduled-tasks/inventory-full-decisions-audit-weekly/SKILL.md:6,11` | **Monday's audit** — next one scheduled, would have failed identically. | ✅ fixed |
| `.claude/settings.local.json` (28 refs, incl. `Read(...Desktop/getplaying/**)`) | **Gitignored** (`~/.config/git/ignore:1`) — that's exactly why the tracked-file sweep missed it. The dead Read grant means permission prompts on ordinary reads. | ✅ fixed, JSON validated |
| `Claude/Projects/Get Playing- Project Brief/.claude/launch.json:7` | Live dev-server launcher — `cd`s into the empty husk, so `npm run dev` fails outright. | ✅ fixed |
| `…/Get Playing- Project Brief/.claude/settings.local.json` (23 refs) | Same dead-permission problem as above. | ✅ fixed |
| `Brady's Workspace/SYSTEM/workflows/_project-registry.md:17` | The hub-side registry row. **DECISIONS.md:31 explicitly flagged this one as still owed** — now closed. | ✅ fixed |

**Verified already-correct** (no action): `.claude/settings.json` SessionStart hooks (`L83`), `.claude/launch.json`, shell rc files, `github-token-renewal` task (no path refs).

---

## 🔧 Assertion hygiene (applied, uncommitted)

Two surface lists in `assertions.md` had been wrong for three weeks. Both edited — **not committed**; regress-watch never auto-commits, so these sit in your working tree for review.

- **`decision-status-cycle-locked`** — added `components/JustFiveMinutes.tsx` and `lib/constants.ts` to the surface list. JFM is where the last two status-label drifts actually surfaced (caught 07-20, still shipping 07-29, fixed 07-30) and it was never enumerated; the assertion found the bug only because the grep happened to run repo-wide. Recommended 07-20 and 07-29, not done either time.
- **`decision-playing-now-cap-3`** — surface was `lib/store.ts`, but the cap lives in `lib/constants.ts:59` and is enforced at three call sites (`Reroll.tsx:222`, `JustFiveMinutes.tsx:118`, `GameCard.tsx:426`). That mismatch produced a "grep-miss" footnote in both the 07-20 and 07-29 audits — a recurring false signal, now removed. Corrected assertion re-run: **passes** against all three sites.

---

## Deliberately left alone

- **In-repo `docs/` mentions of the old path** (`session-resume-*`, `DECISIONS.md`, `archive/`, `specs/web-ios-interop.md:44`). `DECISIONS.md:31` sets the convention explicitly — *"live in-repo refs are swept, historical `docs/` mentions are left as records of the past."* They're accurate history. Not touched.
- **bradyOS vault logs and narrative** (`wiki/inventory-full.md`, `wiki/log.md`, `decision-log.md`, `SYSTEM/logs/*`, `Handoffs/`, `SYSTEM/backups/*`). Dated records; several already state the move correctly. Same rule.
- **`SYSTEM/dashboard/index.html` + `.cache-artifact-current.html`** — generated output, so editing the artifact wouldn't stick. Worth knowing: the dashboard **already tracks this**, item 16 — *"~/Desktop/getplaying still exists beside the new ~/dev/getplaying … confirm both are dead leftovers."* Your hub caught the husk independently.
- **`SYSTEM/dashboard/README.md:18`** — states canonical paths in the present tense (`Desktop/getplaying` for Inventory Full) but sits inside a dated "Moved 2026-05-26" note. Genuinely ambiguous between live pointer and history, so **your call** — one-token fix if you want it live.
- **The husks themselves** — `~/Desktop/getplaying/` (`.DS_Store` + stray `node_modules/`) and `~/Desktop/getplaying-lint/`. Safe to delete as far as I can tell, but deleting is yours to run.

---

## Follow-ups

**Observation, low — noting once, will not re-flag:**
- The JFM triage buttons don't follow one convention. `🎯 Up Next` (L262) and `📚 Back to The Pile` (L269) match canon icons; `🔥 Playing this now` (L255) uses a non-canon icon (canon is `▶️`) and sentence-form phrasing instead of the `Playing Now` label. Not a violation — "Playing this now" isn't on the retired list, and "The Pile" is explicitly permitted (`voice-charter.md:72`). Reads as deliberate action-phrasing. Flagging once so it's a decision rather than a drift; if intentional, no action and I won't raise it again.

**Still-open gap from 07-29** (decisions-audit's territory, restated so it doesn't get lost): no `decision-*` assertion yet for the **2026-05-20 imports-default-to-Backlog** lock — the highest-value missing check, with a recurring violation history. Monday's audit owns it.

---

*Autonomous weekly run, default mode, then a follow-on path sweep at Brady's request. Grep-primitive checks only; no preview server started. **No product/UI code modified. Nothing committed, nothing pushed.** Edits were confined to config, agent memory, hub pointers, and two `assertions.md` surface lists — the last of which are uncommitted in the working tree for review.*
