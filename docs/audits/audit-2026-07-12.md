# Regress Watch — 2026-07-12 · changed-files (light pass)

**Mode:** named-bug regression catalog (default). NOT decisions-audit — Monday's `inventory-full-decisions-audit-weekly` owns that; not duplicated here.

## Scope

`git log --since="8 days ago"` → **one commit, non-visual:**

- `1606000` — *ci: run verify.sh gate (lint + typecheck + build) instead of bare build* — touched only `.github/workflows/ci.yml` (+`verify.sh`).

No `components/`, `app/`, `lib/`, or asset changes this week. Per skill "When NOT to run" (pure tooling/data-layer changes have nothing visual to regress), the preview-driven catalog is not warranted. Ran only the cheap grep smoke checks that don't need a server.

## Checks run

| Assertion | Method | Verdict | Note |
|---|---|---|---|
| `og-no-webp-img` | grep `app/**/opengraph-image.tsx` | ✅ PASS | No `<img src="*.webp">` in either OG route |
| OG route files present | `ls` | ✅ PASS | `app/pile/[id]/opengraph-image.tsx`, `app/clear/[id]/opengraph-image.tsx` both present |

## Not run (no triggering surface changed this week)

All preview-driven `regress-*` (wordmark integrity, header double-stack, tabnav sprites/teal, void-theme AA) and OG render smokes (`og-pile-renders`, `og-clear-renders`) — no wordmark, header, tab, theme, or OG-runtime code moved. Nothing to regress against.

## Summary

```
Regress Watch — 2026-07-12 · light (CI-only week)
✅ 2 grep smokes checked, 2 passed
❌ 0 hard fails
⚠️  0 soft fails
🔍 0 false positives
```

Quiet week — only tooling moved. No visual regressions possible from this diff. Next substantive pass warranted when a commit touches `components/`, `app/`, `lib/`, or assets.
