# Decisions Audit — 2026-05-25

Agent B weekly run. Verifies that LOCKED entries in `docs/DECISIONS.md` are still reflected in shipped code. Drift surfaced, not fixed — Brady decides what to act on.

**Scope:** 6 `decision-*` assertions audited (each maps to a LOCKED decision). DECISIONS.md holds ~20+ locked entries; the remaining ones have no assertion yet (listed at the bottom). Verification used the cheapest primitive available autonomously — `grep` + targeted file reads. No live preview server was started (the skill says ask Brady first), so the `eval`/`vision`/live-keypress clauses in a few assertions are marked **not live-verified** and lean on static reads instead.

**Verdict tally: 3 holds · 1 hold-with-note · 2 drift (1 unauthorized, 1 authorized-stale-assertion).**

---

## Per-assertion verdict

### `decision-status-cycle-locked` — ❌ Drift (unauthorized)
Internal status keys intact: `buried | on-deck | playing | played | bailed` (`lib/types.ts:3`). No retired labels in store/type logic.
**But:** `components/JustFiveMinutes.tsx:262` renders a button labeled **"📋 Play Next"** — a retired label (canonical is **Up Next**). The handler correctly calls `handleTriage('on-deck')`, so the *behavior* is right; only the user-facing string regressed. No DECISIONS entry authorizes reintroducing "Play Next" (it was explicitly retired 2026-04-09, line 1172). One-word fix: "Play Next" → "Up Next".

### `decision-picker-cta-pick-my-game` — ⚠️ Drift (copy-sweep gap)
Primary CTA is correctly **"Pick My Game"** at every CTA site: `app/page.tsx:740`, `components/PostImportSummary.tsx:143`, `components/Reroll.tsx:483` (+ `aria-label` :463), `app/layout.tsx:180` (JSON-LD). Core decision holds.
**But:** the old label **"What Should I Play?"** still appears in user-facing copy, which the assertion forbids in *any* user-facing context:
- `components/HelpModal.tsx:47, 65, 69, 116` (FAQ title + body)
- `components/PostImportSummary.tsx:102` (body copy points at a button now labeled "Pick My Game" — internally inconsistent)
- `app/privacy/page.tsx:50, 280` (legal copy describing the feature)
- `app/page.tsx:748` is a code comment, not user-facing — ignore.
The 2026-05-05 rename entry scoped itself to the CTA sites only; help/privacy/post-import body were never swept. Not a CTA regression — a descriptive-copy lag. The `R`-key-opens-picker clause was **not live-verified** (no preview).

### `decision-moving-on-canon` — ✅ Holds
Moved-On surfaces affirm agency, never frame as failure. Cited files carry positive variations: `components/GameCard.tsx:548` (Moved On + affirmation), `:1289` ("I'm moving on"), `:1296`, `:1306` ("Yeah, not for me"); `components/CompletionCelebration.tsx:240` ("got the time it deserved. Moving on."). The exact anchor "Moving on is deciding too" lives on the landings (`LandingPageV2.tsx:466`, `LandingPageClassic.tsx:271`) — assertion explicitly allows variations as long as sentiment holds. No shame vocab (`quit on it` / `abandoned` / `dropped`) in any of the three cited files.

### `decision-pick-flow-two-inputs` — ✅ Holds
`components/Reroll.tsx` surfaces exactly two input dimensions: **mood** (`moodFilters`, `ALL_MOODS`, `toggleMood`) + **session length** (`SessionLength`, `sessionLength`, `showSessionLengthDrawer`). `EnergyLevel` is fully renamed to `SessionLength` per the 2026-04-27 PM pivot. No genre or platform input group at default — multi-select mood is explicitly SHELVED (`:110`), platform comes from settings (`:102`), not a pre-roll control. Session-length drawer collapsed by default per the locked rule (`:74–76`). Live count was **not live-verified** but the static control inventory is unambiguous.

### `decision-playing-now-cap-3` — ✅ Holds (one soft copy note)
`lib/constants.ts:59` `MAX_PLAYING_NOW = 3`, and it is **enforced at every promotion entry point** (matches the 2026-04-06 entry title): `components/Reroll.tsx:234`, `components/JustFiveMinutes.tsx:118`, `components/GameCard.tsx:424` & `:565`, `app/page.tsx:317`, plus the cap-reached banner at `app/page.tsx:880–882` ("The constraint is the feature."). Each blocks the 4th promotion with a toast. Strong hold.
**Soft note:** `components/HelpModal.tsx:47` says "You can have multiple games here. We don't judge." — true (up to 3 is multiple), but it soft-pedals the hard cap. Not drift; flag only if a user is ever surprised by the cap toast.

### `decision-tagline-canon` — ⚠️ Drift (authorized — assertion is stale)
Decision canon **holds in code**: "get playing." present (`app/about/page.tsx:59, 60, 395`, etc.); "Stop stalling" absent from all user-facing surfaces (only in the DECISIONS log).
**The assertion itself is out of date.** It claims the landing subhead is "Your pile's not gonna play itself." — but `voice-charter.md` was updated **2026-05-11**: the landing H1 is now "Your backlog isn't the problem. Deciding is." The live landing is `LandingPageV2` (`app/page.tsx:30` imports it as `LandingPage`), which renders "…Deciding is." (`LandingPageV2.tsx:241`). The old subhead survives only in `LandingPageClassic.tsx:164` (not rendered) and the pile share card `app/pile/[id]/page.tsx:255`. Action: update this assertion's subhead clause to match the 2026-05-11 charter, and point its surface at `LandingPageV2`.

---

## Suggested follow-ups

**Refine (stale assertions):**
- `decision-tagline-canon` — replace the "Your pile's not gonna play itself." subhead clause with the 2026-05-11 charter H1 ("Your backlog isn't the problem. Deciding is."); retarget surface from the retired `LandingPage.tsx` to `LandingPageV2.tsx`.
- All six assertions still name `components/LandingPage.tsx` implicitly or cite pre-split paths — that file no longer exists. Landing is now `LandingPageV2.tsx` (live) + `LandingPageClassic.tsx` (dormant). Update surface lists.

**Fix candidates (drift):**
- `JustFiveMinutes.tsx:262` "Play Next" → "Up Next" (one-word, unauthorized label regression).
- Decide whether the picker-CTA copy sweep should extend to `HelpModal.tsx`, `PostImportSummary.tsx:102`, and `privacy/page.tsx`. If yes, swap "What Should I Play?" → "Pick My Game" there; if the old name is intentional in legal/help context, narrow the assertion's "any user-facing context" clause to "any CTA or primary-action context."

**No assertion yet — add one if it should be audited weekly:**
- **User agency / never auto-assign status** (LOCKED, line 1481 + AGENTS.md principle #7) — highest-value gap. Recurring violation pattern (Xbox, Steam HLTB both rolled back). A `decision-no-auto-status` grep assertion (imports default to `buried`/Backlog; no heuristic sets `played`/`on-deck`/`bailed`) would catch the exact regression that's bitten three times.
- **Similar-games only on Completed modal** (2026-05-05) — auditable via grep for the `status === 'played'` gate in `GameCard.tsx`.
- **Modal destructive-action disclosure** (2026-05-05) — Delete/Don't-Suggest behind "··· More actions"; Bail always visible.
- **Share-card content lockdown** (2026-04-27 / 2026-04-21) — `$ reclaimed` framing, no per-share customization, footer CTA.
- **DONE_FLAVORS for non-finishable games** (2026-05-13) — `CompletionCelebration.tsx` branches share flavor on `isNonFinishable`.
- **GA4 dataLayer init in root-layout head** (2026-05-01) — grep that the inline init lives in `app/layout.tsx`, not back inside `CookieBanner`.

**Skip (correctly un-asserted):** iOS app (2026-05-20, separate repo — not in this tree); "Enjoy your games again" mission (2026-04-26, too abstract for a check, per assertion-authoring guidance).

---

*Run mode: `regress-watch` decisions-audit (Agent B), autonomous scheduled run. Primitives: `grep` + targeted `Read` only — no live preview server started. Commit SHA at run time: `1ae1678` (repo had one untracked file, `public/if-logos/inventory-full-logo-1024-square.png`; audited against working-tree state). Date: 2026-05-25 PDT.*
