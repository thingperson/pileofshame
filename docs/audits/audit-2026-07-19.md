# Regress Watch — 2026-07-19 · default (named-bug) mode

**Scope:** surfaces touched in the last 8 days. Changed user-facing code:
`components/LandingPageV2.tsx` (landing, heavy), `app/page.tsx` + `app/layout.tsx` (shell),
`components/CookieBanner.tsx` (removed), `components/Reroll.tsx` (Smart Pick % copy fix).
Data-layer only (skipped, nothing visual to regress): `lib/analytics.ts` (GA4→Vercel), `lib/smartPickCopy.ts`, `supabase/migrations/002_*`.

Focused the catalog on landing + in-app hub + OG smoke. Decisions-audit (`decision-*`) NOT run — that's Monday's task.

```
Regress Watch — 2026-07-19 · default/changed-scope
✅ 10 assertions checked, 10 passed
❌ 0 hard fails
⚠️  1 soft (review) — retired-tagline echo on pile share card
🔍 1 assertion-refinement candidate (stale threshold)
```

## Results

| Assertion | Surface | Verdict | Note |
|---|---|---|---|
| `regress-no-redundant-wordmark` | landing | ✅ PASS | Empty-state renders only LandingPageV2; one `<Wordmark variant="full">` in header (`LandingPageV2.tsx:123`); hero is text H1, no hero wordmark. No DefaultBanner co-render (`app/page.tsx:579-607`). |
| `regress-header-no-double-stack` | landing | ✅ PASS | Single `flex justify-between` header row (`LandingPageV2.tsx:119`). No stacked rows / black separator. |
| `header-single-row-on-desktop` | hub + landing | ✅ PASS | Hub screenshot: wordmark + Import/Stats/Sign in/gear in one row, no wrap (~1600px). Landing header structurally single-row. |
| `regress-wordmark-no-orphan-punctuation` | hub | ✅ PASS | Hub tagline wordmark renders "INVENTORY FULL / GET PLAYING" — no orphan ".gg", no clipped glyphs (vision). Landing uses `variant="full"`, not the at-risk `tagline` variant. |
| `regress-wordmark-not-bungee-fallback` | OG | ✅ PASS | OG routes render as PNG (below); routes unchanged this week. Brand-path not re-inspected — unchanged surface. |
| `tabnav-sprites-render` | hub | ✅ PASS | Each tab = 1 `<svg>` w/ brand fills (not emoji). See refinement note. |
| `regress-tabnav-muted-teal-v2.1` | hub | ✅ PASS | `#1a9e86` present in tab sprites; banned `#2ee8c4`/`#34d399` absent (eval on computed fills). |
| `og-pile-renders` | `/pile/test-id/opengraph-image` | ✅ PASS | 200, `image/png`, 22994b (edge runtime). |
| `og-clear-renders` | `/clear/test-id/opengraph-image` | ✅ PASS | 200, `image/png`, 20557b — Node-runtime `fs.readFile` path intact. |
| `og-no-webp-img` | OG route sources | ✅ PASS | All 4 `opengraph-image.tsx` clean of `.webp`. (webp hits exist only in `app/about` + `app/why-deciding-is-hard` via `next/Image`, not OG/satori.) |

Bonus check (07-15 cookie-banner removal): no dangling `CookieBanner` refs in `app/` or `components/`. Clean.

## ⚠️ Soft — review (not this mode's job to fail on)
- `app/pile/[id]/page.tsx:255` — **"Your backlog's not gonna play itself."** This is a survivor of the retired
  "Your pile's not gonna play itself." subhead family (voice-charter retires it; landing H1 is now
  "Your backlog isn't the problem. Deciding is."). It's on the **pile share card** (unchanged this week), not the landing,
  so not a landing regression — but it's `decision-tagline-canon` territory. **Flag for Monday's decisions-audit.**

## 🔍 Assertion-refinement candidate
- `tabnav-sprites-render` threshold `svg path > 5` is **stale**. Live tabs render 4/4/6/4 paths — 3 of 4 fall under the
  threshold yet clearly render as SVG sprites with brand fills (not emoji). Suggest: `≥1 svg with ≥1 path AND a brand fill present`,
  or lower to `> 3`. Not a bug — the assertion would false-positive as written.

## Coverage notes
- **Live empty-state landing not screenshotted:** this machine's local store auto-seeds the sample library on empty
  (`getplaying-library` re-populates after clear + reload), so LandingPageV2's empty state was verified from source +
  the shared Wordmark confirmed on the in-app hub screenshot. Not a code issue — a local-fixture quirk.
- **Not run (other skills / unchanged):** `regress-void-theme-aa` (theme untouched → `/theme-check`),
  `regress-hero-not-dino` (no comp this week), all `decision-*` (Monday's decisions-audit).

*No auto-fixes. No push. Surface-only, per skill.*
