# Regress Watch — 2026-07-07

**Mode:** `regress-watch` named-bug regression catalog (default mode, weekly cadence). *Not* the decisions-audit — that ran separately as `audit-2026-07-06.md` (Agent B). No overlap.
**Scope:** changed-files. `git log --since="8 days ago"` (2026-06-28 → today). Verification by `Bash` grep / asset-existence / code-read only — **no live preview** (autonomous run; SKILL.md says don't start a dev server unprompted, collision risk with Brady's local instance).

**Headline:** 5 static assertions PASS, 0 hard fails, 3 NEEDS-HUMAN (all live-only visual checks on this week's changed surfaces). Nothing statically regressed.

---

## What changed this week (surface triage)

| Commit | Surfaces | Visual? |
|---|---|---|
| e39c0d3 (06-29) | Reroll (mood banner), globals.css (90s chrome), Xbox/Playnite import modals, 10 mood PNGs | ✅ yes |
| 81a6e79 (06-29) | LineIcon rollout — Reroll, CompletionCelebration, SettingsMenu, StatsShareComposer, ValueCalculator | ✅ yes |
| 1aca880 / 81a7c18 (06-29) | Share-card overhaul + stats hero — CompletionCelebration, GameCard, StatsPanel, stats page | ✅ yes |
| 274c187, 1027b91, 1b40dfe, others (06-30 → 07-06) | statusEvents, recommendations, store, CloudSync, useAutoEnrich, enrichGame, CookieBanner, PostImportSummary, verify.sh/ci | ❌ data-layer / server — no visual regress surface |

Data-layer commits skipped per SKILL.md "When NOT to run."

---

## Per-assertion verdict

| Assertion | Surface | Verdict | Evidence |
|---|---|---|---|
| `og-no-webp-img` | OG routes | ✅ PASS | grep `src=*.webp` in `app/pile`, `app/clear` → none |
| mood-sprites-present *(asset-check)* | `public/moods/` | ✅ PASS | all 10 PNGs present (atmospheric…strategic) |
| mood-sprites-wired *(404 guard)* | [Reroll.tsx:576](components/Reroll.tsx:576) | ✅ PASS | banner src `/moods/${moodFilters[0]}.png`; `ALL_MOODS` ([Reroll.tsx:22](components/Reroll.tsx:22)) keys ⇔ 10 filenames exactly. No 404. |
| `regress-wordmark-not-bungee-fallback` | share-card path | ✅ PASS | no `bungee` / system-font-family in CompletionCelebration, launch.ts, GameCard |
| `regress-void-theme-aa` | void theme tokens | ✅ PASS (N/A) | globals.css change was **90s-theme** chrome, not void tokens. Void untouched. |
| 90s-hub-chrome-legibility | `.theme-90s` header + inactive tabs | 🔍 NEEDS-HUMAN | New contrast fix ([globals.css](app/globals.css) `.theme-90s button.text-text-dim` → `#333`, header btn → `#000`). CSS looks sound; needs 90s theme rendered to confirm live. |
| import-modal-overflow | Xbox / Playnite modals | 🔍 NEEDS-HUMAN | `overflow-hidden` + `overflow-y-auto` added for mobile-height clipping — vision/live-only |
| mood-banner-render | Reroll vibes drawer | 🔍 NEEDS-HUMAN | 96px selected-state banner + gradient fade — asset wiring verified statically, visual polish needs eyes |

---

## Not re-checked (unchanged surfaces, low risk)

Landing header double-stack, hero wordmark orphan-punctuation, tabnav sprite render, tabnav muted-teal — **none of those surfaces changed this week**, so their named regressions can't have reappeared from this week's diff. Skipped to keep the pass light per the scheduled-task instruction.

---

## Suggested follow-ups (for Brady, no action taken)

- The 3 NEEDS-HUMAN items are all this week's *new* visual work (90s chrome fix, import overflow fix, mood banner). They were presumably eyeballed on 06-29 when shipped — this is just the regression net noting they can't be statically confirmed. Next time a dev server is up, a 60-second live pass on 90s theme + a mobile-height import modal closes them.
- No new `regress-*` assertion warranted — nothing broke.
